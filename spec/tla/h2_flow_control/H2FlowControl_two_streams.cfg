\* expect: holds
\* Three frames in flight each way: four holds too, over 936,271 states, at more than twice the time.
SPECIFICATION Spec
CONSTANTS
    Client = client
    Server = server
    Nobody = nobody
    ChannelMax = 3
    Max = 4
    InitialWindow = 2
    Streams = {1, 3}
    Body = 2
    Threshold = 1
    Changer = nobody
    NewInits = {}
    ChangesMax = 0
    HonourNegative = TRUE
    DropOnEnd = TRUE
INVARIANTS TypeOK NoForbiddenFrame NoFlowError
PROPERTIES Finishes
CHECK_DEADLOCK FALSE
